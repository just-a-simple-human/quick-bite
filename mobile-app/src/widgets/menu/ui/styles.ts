import { StyleSheet } from "react-native";

export const menuStyles = StyleSheet.create({
  wrapper: {
    paddingHorizontal: 16,
  },
  columnWrapper: {
    gap: 12,
  },
  column: {
    gap: 16,
  },
});
