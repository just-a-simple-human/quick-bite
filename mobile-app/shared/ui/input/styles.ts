import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    backgroundColor: "transparent",
    gap: 8,
  },
  label: {
    fontSize: 18,
    fontWeight: "500",
  },
  inputField: {
    padding: 16,
    borderRadius: 12,
    borderWidth: 1,
    fontSize: 16,
    fontWeight: "400",
  },
});
